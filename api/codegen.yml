overwrite: true
schema: "http://localhost:4001"
documents: null
generates:
  src/generated/graphql.ts:
    plugins:
      - "typescript"
      - "typescript-resolvers"
      - "typescript-mongodb"
    config:
      avoidOptionals: true
      # mappers:
      #   Player: ../backend/src/user#UserModel
  src/generated/graphql.schema.json:
    plugins:
      - "introspection"
